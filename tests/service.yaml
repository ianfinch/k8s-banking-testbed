apiVersion: v1
kind: Service
metadata:
  labels:
    app: tests
  name: tests-service
spec:
  ports:
  - port: 80
    targetPort: 3000
    appProtocol: http
  selector:
    app: tests-pod
  type: ClusterIP
